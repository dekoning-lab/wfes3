#ifndef WFES_CLI_PARSING_H
#define WFES_CLI_PARSING_H

// Define WFES_CLI to allow conditional compilation (only if not already defined)
#ifndef WFES_CLI
#define WFES_CLI
#endif

// Include the original parsing.h, which will conditionally include Qt
#include "../../wfes-lib/source/utils/parsing.h"

#endif // WFES_CLI_PARSING_H